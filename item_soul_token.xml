<!-- app/src/main/res/layout/item_soul_token.xml -->
<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="240dp"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="8dp"
    app:cardBackgroundColor="#1A0033"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    app:strokeColor="#FF00FF"
    app:strokeWidth="1.5dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <!-- Soul/Aura Image (loaded from IPFS via Coil) -->
        <ImageView
            android:id="@+id/iv_token_image"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:contentDescription="Soul Token Image"
            android:background="@drawable/gradient_soul_energy"  <!-- fallback gradient -->
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/guideline_image_bottom"
            app:layout_constraintVertical_weight="0.65" />

        <!-- Token ID Badge -->
        <TextView
            android:id="@+id/tv_token_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="#0001"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:background="@drawable/badge_token_id"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            app:layout_constraintTop_toBottomOf="@id/iv_token_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="12dp" />

        <!-- Soul Name / Type -->
        <TextView
            android:id="@+id/tv_soul_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Eternal Flame Soul"
            android:textSize="14sp"
            android:textColor="#00FFFF"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/tv_token_id"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="8dp" />

        <!-- Karma / Aura quick stat -->
        <TextView
            android:id="@+id/tv_stat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Karma: +137 | Aura: Violet"
            android:textSize="12sp"
            android:textColor="#AAAAAA"
            android:gravity="center"
            app:layout_constraintTop_toBottomOf="@id/tv_soul_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="8dp" />

        <!-- Small chain icon or verified badge -->
        <ImageView
            android:id="@+id/iv_blockchain_badge"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_ethereum_badge"  <!-- add your own icon -->
            android:contentDescription="On-chain verified"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="12dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Horizontal guideline to control image height ratio -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_image_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.65" />

</com.google.android.material.card.MaterialCardView>
