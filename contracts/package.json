// package.json (root or /contracts/package.json - run npm init -y and add)
{
  "name": "demon-core-contracts",
  "version": "1.0.0",
  "scripts": {
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
